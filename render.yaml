services:
  - type: web
    name: fatballot-api
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run server
    healthCheckPath: /api/offices
    envVars:
      - key: NODE_ENV
        value: production
    plan: free
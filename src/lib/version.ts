// Single source of truth for the deployed build stamp.
// Bump on every production release commit so the live version is readable
// in the UI footer and in GET /api/health (frontend AND backend must match).
export const APP_VERSION = '2026-09-18-rel1';
